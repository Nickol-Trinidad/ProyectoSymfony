<form method="post" action="{{ path('app_support_ticket_delete', {'id': support_ticket.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ support_ticket.id) }}">
    <button class="btn">Delete</button>
</form>
